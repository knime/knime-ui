/**
 * Sentinel option ID appended by the backend to choice inquiries, enabling the
 * user to type a freeform answer instead of picking only from predefined
 * options. The backend strips this ID before returning results to the agent.
 */
export const FREEFORM_OPTION_ID = "reserved::freeform";
