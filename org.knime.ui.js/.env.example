# Copy this file to `.env` to activate it.
# The given values in this file are the defaults.

# port where the frontend app would run
VITE_APP_PORT=3000

# port of the locally running AP that starts the Websocket Gateway for dev
VITE_WS_API_URL="ws://localhost:7000/gateway"