// A difference between the "changeNodeStates" could be asserting more specifically
// if items are enabled, even if you are not using them.

// Actually, this one looks useless. I could do it, but it's just to get coverage. Doesn't add value.
